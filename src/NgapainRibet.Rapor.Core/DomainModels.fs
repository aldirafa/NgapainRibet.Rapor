namespace NgapainRibet.Rapor.Core

/// <summary>
/// Placeholder domain models — sengaja dibuat minimal.
/// Desain lengkap untuk DownloadState, AiState, Student, dsb.
/// (sesuai spesifikasi proyek bagian 4) akan dikerjakan sebagai
/// langkah terpisah, bukan bagian dari scaffolding ini.
/// </summary>
module DomainModels =

    /// Placeholder sementara — akan digantikan oleh model domain
    /// yang sesungguhnya (Student, DownloadState, AiState, dll).
    type ScaffoldPlaceholder = { Note: string }
